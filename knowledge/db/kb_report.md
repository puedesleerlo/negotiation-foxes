# Knowledge-base status (Part 1)

Generated by `python -m kb.report` on 2026-09-06. Every number comes from the artifacts, not from the plan.

## Kosmos ingestion

- reports processed: 1 (`Edison Playground.pdf`)
- cited claims extracted: **77**
- references: **49** (12 with a formal entry in the report, 37 only named in the body)
- distinct works after deduplication: **24**
- references in scope L1/L2 (the ones that feed E1's catalog): 35; the rest belong to L3 (credit assignment), out of E1's scope

The report's reference section is cut off inside entry `[12.3]`, so groups `[13.1]`–`[23.1]` exist only as mentions in the body. That is why the ingestor extracts both kinds and the metadata resolution works with both.

## Metadata resolution

| Status | References |
|---|---|
| `resolved` | 34 |
| `ambiguous` | 15 |

Resolved with confidence: **34/49** (69%). The `ambiguous` ones are in `knowledge/db/review_queue.md` with their citation context, so the review does not require opening the PDF.

## PDF download

| Status | References |
|---|---|
| `missing` | 23 |
| `downloaded` | 22 |
| `paywalled` | 4 |

PDFs obtained through legitimate routes: **14 distinct works** of 24. Sources used: OpenAlex open-access locations, arXiv, Semantic Scholar and the direct links carried by the report itself. No paywall was circumvented: whatever has no open route stays in the review queue for download with institutional access.

**Rule applied:** nothing is downloaded for a reference whose metadata is not resolved. Downloading by title search without confirming author and year attaches the wrong PDF to the reference, which is worse than having none.

## Text and database

- papers with section-wise extracted text: 14
- sections indexed: 681
- extraction quality: low=2, ok=12
- database: `knowledge/db/papers.sqlite` with a full-text index (FTS5) over the sections

## Catalog

- foxes: **10** — 6 `validated`, 2 `implemented`, 2 `candidate`
- tools: 8 — 8 `implemented`

| Fox | Status | Estimates | Beats the control |
|---|---|---|---|
| `f01_bayes_rv_concession` | validated | theta.rv | yes |
| `f02_concession_issue_weights` | validated | theta.w | yes |
| `f03_time_concession_regression` | validated | theta.beta, theta.T, theta.rv | yes |
| `f04_accept_boundary_kde` | implemented | p_accept | no (see report) |
| `f05_case_reader_prior` | candidate | theta.rv, theta.w, ranking of interests | not evaluated |
| `f06_uninformed_prior` | validated | theta.rv, theta.w, theta.beta, theta.T | yes |
| `f07_zopa_pareto_estimator` | validated | zopa, pareto_frontier, nash_point, joint_gains | yes |
| `f08_outcome_simulator` | implemented | outcome_distribution, p_impasse | no (see report) |
| `f09_hypothesis_issue_weights` | validated | theta.w | yes |
| `f10_impasse_risk` | candidate | p_impasse | not evaluated |

The full validation reports are in `foxes/<fox_id>/validation_report.md`.
